[build]
  # Set the base directory to client folder
  base = "client"
  # Build command
  command = "npm run build"
  # Output directory (relative to base)
  publish = "dist"

[[redirects]]
  # Redirect all routes to index.html for client-side routing
  from = "/*"
  to = "/index.html"
  status = 200


