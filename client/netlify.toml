[build]
  # Build command
  command = "cd client && npm run build"
  # Output directory
  publish = "client/dist"

[[redirects]]
  # Redirect all routes to index.html for client-side routing
  from = "/*"
  to = "/index.html"
  status = 200